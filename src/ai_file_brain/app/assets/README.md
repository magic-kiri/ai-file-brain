# App assets

Drop a `tray_icon.ico` file in this directory for the system tray and window icon. If absent, the app falls back to a built-in Qt computer icon, and the PyInstaller build skips the `--icon` flag.

Recommended: 256×256 multi-resolution `.ico` (256, 64, 48, 32, 16).
